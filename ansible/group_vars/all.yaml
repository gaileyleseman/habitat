---
# `components` lists the optional roles to apply; each value names a role
# under roles/. The `git` and `shell` roles are always applied.
# Per-machine-type profiles live in group_vars/laptops.yaml and
# group_vars/raspberrypis.yaml; base_packages / base_extra_apt_packages
# default in roles/base/defaults/main.yaml.
components: []
